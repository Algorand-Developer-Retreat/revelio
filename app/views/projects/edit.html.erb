<% content_for :title, "Editing project" %>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing project</h1>

  <%= render "form", project: @project %>

  <%= link_to "Show this project", project_path(@project.abbreviation), class: "ml-2 rounded-md py-2 px-3 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to projects", projects_path, class: "ml-2 rounded-md py-2 px-3 bg-gray-100 inline-block font-medium" %>
</div>
